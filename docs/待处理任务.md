# meeting
## 底部增加快捷操作按钮并附上快捷键。
1. 添加快捷键--切麦/闭麦

## 原文流模块
1. 顺序要倒置，新d在最上面。
2. 在录音后静音一段时间后，再次录音的时候不转写内容。
3. 数据入库的时候没有带上isFinal。
4. 音频累积时长要根据说话者的连贯性来动态改变。应该有一套算法。前端比如一直按 3 秒发送录音，后端进行拼接，当闭包或静音的时候进行转写，这样词句才会完整。比如：A 发言录制3S->3s-3s-3s,这是一个 12 秒完整的句子，应该进入同一条记录，而不是4 条记录。---未实现

## 语句拆分
1. 记录顺序倒置，新的内容在最上面。
2. 当前的逻辑不对需要重构。首先语句拆分助手从transcript_events表中根据设定的窗口值获取数据上下文数据，打包推送给语句拆分服务（GLM），返回结果保存到新的transcript_events_segments表中。
3. 语句拆分服务的工作核心：当被触发且得到上下文内容的时候，获取本会话中上一句的内容A，当前上下文内容内容B，交给 LLM 推理出下一句的内容C，并落库新记录C。 当再次触发的时候，获取本会话中上一句的内容C，当前上下文内容内容B1，交给 LLM 推理出下一句的内容D，并落库新记录D。——循环。（这样在前端就能够看到完整的对话内容。）
